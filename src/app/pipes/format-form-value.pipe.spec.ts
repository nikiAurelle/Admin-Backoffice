import { FormatFormValuePipe } from './format-form-value.pipe';

describe('FormatFormValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFormValuePipe();
    expect(pipe).toBeTruthy();
  });
});
