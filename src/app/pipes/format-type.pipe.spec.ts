import { FormatTypePipe } from './format-type.pipe';

describe('FormatTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTypePipe();
    expect(pipe).toBeTruthy();
  });
});
